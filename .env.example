# Database configuration for DataJeopardy Security Dashboard
# Copy this file to .env and replace placeholder values before running the server.

# MySQL/MariaDB host
DB_HOST=localhost

# Database user with appropriate privileges (do NOT use production root credentials)
DB_USER=your_db_user

# Database user's password
DB_PASS=your_db_password

# Database name
DB_NAME=DataJeopardyDB

# API server port
API_PORT=5000

# Auto-lock threshold (RiskScore). Users with RiskScore >= this value will be auto-locked by background logic
AUTO_LOCK_RISK_THRESHOLD=60

# Example:
# DB_HOST=127.0.0.1
# DB_USER=app_user
# DB_PASS=strongpassword
# DB_NAME=DataJeopardyDB
# API_PORT=5000
# AUTO_LOCK_RISK_THRESHOLD=60
